#!/usr/bin/python3

import image_manipulation.cropper as crp
import image_manipulation.splitter as split
import image_manipulation.scanner as scn

if __name__ == "__main__":
    
    ## Cropping function
    # crp.crop_all()


    # # Splitting function
    # proportion = 0.33 # Default value
    # proportion = float(input("Proportion of test images (float or int) : "))
    # split.split(('true','false'), proportion, nb_shuffle=200)

    # """
    # SCANNER FUNCTION (WARNING : Creates a sh*t ton of images, so check twice before running)
    # """
    # scn.scan()
    
